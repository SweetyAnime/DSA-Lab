AIM:
Write a python program to draw multiple lines using plot() function.

ALGORITHM:
Step 1:Compute the x and y coordinates for points on a sine and cosine curve 
Step 2: Plot the points using matplotlib
Step 3:Display the output 
Step 4: Stop

PROGRAM:
import numpy as np
import matplotlib.pyplot as plt
x = np.arange(0, 3 * np.pi, 0.1)
y_sin = np.sin(x) 
y_cos=np.cos(x)
plt.plot(x, y_sin)
plt.plot(x, y_cos) 
plt.xlabel('x axis label') 
plt.ylabel('y axis label') 
plt.title('Sine and Cosine') 
plt.legend(['Sine','Cosine']) 
plt.show()

RESULT:
Thus the python program multiple line using plot function has been implemented and executed successfully
